export interface Conge {
    // id: number;
    numcin: number;
    dateDebut: string;
    dateFin: string;
    raison: string;
    typeConge: string;
    // statut: 'en attente' | 'accepté' | 'refusé';
    // employeId: number;
    // createdAt: string;
    // updatedAt: string;
}
